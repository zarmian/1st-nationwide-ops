import { ComingSoon } from "../_components/ComingSoon";

export default function KeysPage() {
  return (
    <ComingSoon
      title="Keys"
      blurb="Single register of every key, fob, and code we hold. Today these live on each site’s detail page; this view will surface them across all sites with chain-of-custody history."
    />
  );
}
