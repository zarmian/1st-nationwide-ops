import { ComingSoon } from "../_components/ComingSoon";

export default function OnboardingPage() {
  return (
    <ComingSoon
      title="Onboarding pipeline"
      blurb="Sites in survey / front-key / shutter-key / alarm-fob / go-live. Today the data is in OnboardingPipeline rows; this view will track each site through the stages."
    />
  );
}
