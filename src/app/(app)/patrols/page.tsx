import { ComingSoon } from "../_components/ComingSoon";

export default function PatrolsPage() {
  return (
    <ComingSoon
      title="Patrols"
      blurb="Patrol schedules, routes, and visit history. Right now patrol activity is captured via /submit and shows on the site detail page."
    />
  );
}
